import styled from 'styled-components';
import { TableCell } from '@material-ui/core';

const StyledHeader = styled(TableCell)`
  background-color: #4a40bf !important;
  color: #fff !important;
  font-weight: 700 !important;
`;

export default StyledHeader;
